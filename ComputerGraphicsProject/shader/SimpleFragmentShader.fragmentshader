#version 330 core

// Interpolated values from the vertex shaders
in vec2 fragmentColor;

// Ouput data
out vec3 color;

// Values that stay constant for the whole mesh.
uniform sampler2D texture_diffuse1;

void main()
{

	color = texture( texture_diffuse1, fragmentColor ).rgb;

}